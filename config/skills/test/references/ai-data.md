# AI and data verification

Verify data quality and lineage, schema correctness, representative evaluation coverage, baseline comparison, robustness, uncertainty and fallback behavior, privacy, reproducibility, latency, and cost.

Keep final acceptance cases independent from examples used to tune the system when feasible. Report distributions and failure clusters, not only averages. Separate model, prompt, data, orchestration, and interface causes before routing defects.
