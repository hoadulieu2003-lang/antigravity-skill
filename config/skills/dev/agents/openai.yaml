interface:
  display_name: "Dev (Project Delivery)"
  short_description: "Plan and produce approved deliverables ($dev)"
  default_prompt: "Use $dev to plan, partition, and produce this approved deliverable."
policy:
  allow_implicit_invocation: true
